project('pager', 'c',
  version: '0.1',
  license: 'MPL-2.0',
  default_options: ['warning_level=3'])

# define source files
src = files('pager.c')

exe = executable('pager', sources: src, install: true, dependencies: [

])

